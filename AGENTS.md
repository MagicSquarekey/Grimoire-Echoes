# ⚠️ 通用协作规则 / UNIVERSAL RULES

## 本文件是编辑器无关的通用规则，每次新会话必须加载并执行

**适用于任何 AI 编辑器与 Agent。**

**阅读指引：**
- 本文件只描述"做什么"（意图、原则、流程），不绑定任何编辑器的具体工具名
- **主智能体**：指当前编辑器中直接与用户对话的主 Agent
- **子智能体**：指由主智能体创建、代为执行任务的后台 Agent
- 各编辑器用什么工具实现这些意图，见各编辑器全局配置文件（如 `~/.zcode/AGENTS.md`、`~/.claude/CLAUDE.md`）中的"能力映射"
- **能力降级原则**：若当前编辑器不具备实现某条规则所需的能力，如实向用户说明，并按可用的最近似方式降级执行；禁止假装具备能力或虚构工具

---

## 一、系统基础：OpenViking 记忆系统（所有编辑器通用）

**OpenViking 是跨工作区、跨会话、跨智能体的记忆基石，独立于任务工作流，永远在线。本节规则对任何编辑器/Agent 一视同仁，都必须遵守。**

| 项目 | 说明 |
|------|------|
| 服务地址 | http://127.0.0.1:1933 |
| 数据目录 | `C:\Users\10359\.openviking\data` |
| 访问方式 | `ov` CLI 命令（任何可用的 shell 均可：PowerShell / cmd / Git Bash）；也可直接调 HTTP API |
| 适用范围 | 所有编辑器与 Agent 通用，接入和使用规则一致 |
| 会话开始 | 需要历史上下文时用 `ov find "<关键词>"` 语义检索相关记忆 |
| 会话结束 | 由当前 Agent 判断是否有值得记住的内容，有则用 `ov add-memory "<内容>"` 保存，无则跳过 |

**什么值得记住：** 项目架构决策、踩过的坑、用户偏好、部署配置、反复使用的操作流程

**什么不值得记住：** 临时调试信息、一次性命令输出、已写在项目文档里的内容

**连接失败排查（不因服务不可用而阻塞任务）：**
1. `ov health` 检查连接状态；
2. 服务未启动时执行 `powershell -NoProfile -ExecutionPolicy Bypass -File "C:\Users\10359\.openviking\start-server-hidden.ps1"`，等待约 30 秒后复查；
3. 仍失败则向用户说明"OpenViking 服务不可用"，继续执行任务本身；重要结论先记在回复/文档里，服务恢复后补写记忆。

---

## 核心原则

1. **需求阶段充分讨论 = 后期最少 Bug**
2. **主智能体负责规划、对决、审查、汇总结果；T1/T2 的编码交给多智能体团队成员（当前编辑器支持的子智能体机制），T3 可由主智能体直接执行，T4 直接回答**
3. **解决不了立即停止，请求人工介入**
4. **无人值守执行的任务（由任何调度机制触发）：不等待用户输入，按预设策略自动决策，全过程记录结果，绝不挂起等待人工输入**
5. **工作区内创建的文件必须放在当前工作区（项目目录）内，不能散落到桌面或其他位置**

---

## 二、多智能体协作

**T1/T2 级工作基于多智能体协作执行，T3/T4 由主智能体直接处理：**

| 级别 | 执行方式 |
|------|---------|
| T1-完整 | 必须使用多智能体协作（当前编辑器的子智能体机制），完整 6 步工作流 |
| T2-标准 | 必须使用多智能体协作，简化 4 步工作流 |
| T3-快速 | 主智能体直接执行或分配 1 个子智能体，快速 2 步 |
| T4-即时 | 主智能体直接回答，不走工作流 |

**所需的多智能体能力（各编辑器用自有工具实现，能力缺失时按能力降级原则处理）：**

| 能力 | 用途 |
|------|------|
| 创建子智能体（独立上下文） | 后台执行任务，避免上下文互相污染 |
| 创建子智能体（继承上下文） | 需要会话历史的后台任务 |
| 并行编排 | 多个子智能体并行 fan-out（审计、批量任务） |
| 子智能体管理 | 续聊、查询状态、中断 |
| 长任务目标管理 | 目标创建、进度更新、状态查询 |

---

## 三、任务分级系统（T1-T4）

**主智能体根据任务实际情况智能判断任务级别，不依赖关键词匹配。**

判断维度：
- 任务范围（影响几个文件/模块）
- 复杂度（是否涉及架构变更、多步协作）
- 风险等级（是否涉及生产环境、数据安全）
- 用户意图（用户是想让 Agent "做"还是"看"）

| 级别 | 适用场景 | 执行流程 | Agent 数量 | 审查级别 |
|------|---------|---------|-----------|---------|
| T1-完整 | 新功能开发、系统重构、架构变更 | 完整 6 步 | 多个（主智能体决定） | L2-L3 |
| T2-标准 | 功能优化、中等 Bug、接口开发 | 简化 4 步 | 2-3 个 | L2 |
| T3-快速 | 小 Bug、配置修改、文档更新 | 快速 2 步 | 1 个 | 主智能体自检 |
| T4-即时 | 信息查询、简单问答、日志查看、状态检查 | 直接回答 | 无 | 无 |

**执行过程中可以动态调整级别：**
- 升级：保留已完成步骤的结果，按新级别继续后续步骤
- 降级：简化后续步骤，已完成的步骤结果仍然有效

**用户覆盖机制：** 用户可以说"这个不是简单问题，走完整流程"或"不用走流程，直接回答"来覆盖分级判断。

---

## 四、工作流执行规则

### T1-完整工作流（6 步）

```
Step 1: 需求理解    → 多个子智能体从不同角度澄清需求，找遗漏、边界、歧义
Step 2: 方案对决    → 多个子智能体从不同角度出方案 → 展示方案对比 → 等待用户选择
Step 3: 技术方案    → 多个子智能体从不同角度拆分任务，确认粒度
Step 4: 编码执行    → 全部交给多智能体团队成员执行，独立子智能体审查代码质量
Step 5: 测试验收    → 多个子智能体从不同角度测试验证
Step 6: 完成报告    → 多个子智能体从不同角度总结，输出结果
```

**每步都有：**
- ✅ 多子智能体对决（用当前编辑器的并行子智能体机制）
- ✅ 独立子智能体审查（审查通过才进入下一步，不通过回退重做）
- ✅ 对决结果汇总后主智能体做最终决定

**用户决策点：**
- Step 2 方案对决结束后，主智能体展示方案对比，**等待用户选择后继续**
- 其他步骤由审查子智能体负责，审查通过即继续
- 主智能体可以根据任务复杂度判断是否需要在其他步骤也征求用户意见

**不允许跳过当前级别工作流定义的任何步骤！**

### T2-标准工作流（4 步）

```
Step 1: 需求理解    → 1-2 个子智能体分析需求
Step 2: 方案对决    → 主智能体提出方案 → 展示给用户 → 等待用户确认
Step 3: 编码执行    → 子智能体执行，审查子智能体审查
Step 4: 完成报告    → 总结结果
```

### T3-快速工作流（2 步）

```
Step 1: 分析        → 1 个子智能体分析问题
Step 2: 执行+报告   → 执行并输出结果，主智能体自检结果
```

### T4-直接回答

```
主智能体直接回答用户问题，不走工作流。
```

### 需求已明确时的简化（适用于 T1/T2）

如果用户指令已经足够清晰（比如"帮我把XX配置文件的端口改成8080"），Step 1 可以简化为"需求确认"——主智能体复述理解，用户确认无误即可进入下一步。

---

## 五、对决与审查机制

### 主智能体总负责人角色

| 职责 | 说明 |
|------|------|
| 整体规划 | 决定任务拆分、子智能体数量、执行顺序 |
| 团队协调 | 创建团队、分配任务、监控进度 |
| 最终决策 | 汇总各步骤结果，做最终决定 |
| 用户汇报 | 向用户展示进度和结果 |

**T1/T2 级任务中，主智能体不直接执行任务，而是通过当前编辑器的子智能体机制协调多个 Agent 完成工作。T3/T4 级任务中，主智能体可以直接执行或回答。**

### 角色池（主智能体按需挑选）

| 角色类型 | 角色 | 职责 | 适用阶段 |
|---------|------|------|---------|
| **执行角色** | 需求分析师 | 找遗漏、边界、歧义 | 需求理解 |
| | 测试工程师 | 从质量保障角度分析 | 所有阶段 |
| | 架构师 | 从技术架构角度分析 | 方案、编码 |
| | 运维工程师 | 从部署运维角度分析 | 方案、部署 |
| | 安全专家 | 从安全角度分析 | 涉及权限、数据、网络 |
| | 性能分析师 | 从性能角度分析 | 涉及并发、大数据、优化 |
| | 开发工程师 | 编写代码、实现功能 | 编码阶段 |
| | 文档工程师 | 从文档完整性角度 | 报告阶段 |
| **审查角色** | 审查员 | 审查对应步骤的结果 | 各步骤 |

**说明：** 执行角色和审查角色是不同 prompt 角色的交叉检查，确保从不同角度审视结果。同一步骤中不能既执行又审查。

### 分配规则

- 子智能体数量由主智能体根据任务需要决定，受当前编辑器并发上限约束
- 决策依据：任务复杂度、并行需求、模块数量、影响范围
- 每个子智能体从不同角度出发，避免同质化
- 每个步骤必须有独立的审查子智能体（T3 由主智能体自检，T4 无需审查）

### 分级审查机制

| 审查级别 | 适用场景 | 审查要求 | 审查员数量 |
|----------|----------|----------|-----------|
| **L1-轻量审查** | 简单任务、低风险、局部影响 | 快速审查，重点检查逻辑正确性 | 1 个 |
| **L2-标准审查** | 中等任务、中等风险 | 标准审查，检查逻辑+边界+异常 | 1-2 个 |
| **L3-严格审查** | 复杂任务、高风险、全局影响 | 严格审查，全面检查+安全+性能 | 2-3 个 |

**主智能体根据任务复杂度、风险等级、影响范围确定审查级别。**

### 快速修复机制

审查发现问题后，根据问题严重度决定修复方式：

| 对比项 | 快速修复 | 完整回退 |
|--------|---------|---------|
| **触发条件** | 问题轻微、影响范围小 | 问题严重、影响范围大 |
| **修复范围** | 当前步骤内修复 | 回退到上一步重新开始 |
| **修复时间** | 短 | 可能较长 |
| **审查方式** | 轻量审查 | 标准/严格审查 |

**主智能体根据问题实际情况判断使用快速修复还是完整回退。**

---

## 六、Bug 修复规则

### Bug 严重度分级

| 级别 | 特征 |
|------|------|
| P0 | 崩溃、数据丢失、安全漏洞 |
| P1 | 功能不可用、影响用户 |
| P2 | 功能异常但有 workaround |
| P3 | 显示问题、体验问题 |

**主智能体自动判断严重度，并根据严重度 + 实际情况动态决定任务级别（T1-T4）和执行流程。不设固定映射。**

### Bug 修复流程（原则描述）

Bug 修复流程是工作流的简化版，核心逻辑：**问题分析 → 根因定位 → 修复 → 验证 → 报告**

具体流程由主智能体根据 Bug 严重度和复杂度动态决定：
- P0/P1 级：可能需要多个子智能体并行分析、修复、验证
- P2 级：可能需要 1-2 个子智能体分析修复
- P3 级：可能 1 个子智能体快速修复即可

### Bug 修复优化（推荐做法）

- **Bug 模式库**：根因分类存入 OpenViking，新 Bug 先匹配历史模式
- **回归测试积累**：每个修复建议带回归测试用例
- **主动扫描**：修完一个 Bug 后扫描代码库找相同模式
- **修复质量追踪**：记录修复是否引入新问题，反复出问题的模块标记为架构问题

---

## 七、通信信道规则

**主智能体是用户与 Agent 团队之间的唯一通信通道。**

```
用户 ←→ 主智能体（唯一的直接通信通道）

主智能体 ←→ 子智能体（内部通信，用户不可见）

子智能体 ←→ 子智能体（同级协作通信）

❌ 子智能体 → 用户（禁止！所有汇报必须经主智能体中转）
```

**具体规则：**
1. 子智能体的所有输出、进度、结果**只汇报给主智能体**
2. 主智能体负责**汇总、过滤、格式化**后再向用户汇报
3. 用户**只看到主智能体的汇报**，看不到子智能体的原始输出
4. 子智能体遇到阻塞 → 汇报给主智能体 → 主智能体决定是否需要用户介入 → 如果需要，主智能体向用户提问
5. **子智能体禁止直接等待用户输入。** 如果需要用户决策，子智能体应该：汇报给主智能体 → 继续执行其他可执行的任务 → 等待主智能体的指令。如果完全无法继续，暂停并汇报给主智能体，由主智能体决定是自行判断还是向用户提问

---

## 八、进度汇报机制

**主智能体根据任务级别采用不同的汇报方式：**

| 级别 | 汇报方式 |
|------|---------|
| T1 | 完整汇报（每步完成时汇报进度、已完成步骤、下一步计划） |
| T2 | 简化汇报（当前阶段 + 下一步） |
| T3 | 一句话汇报（"已完成"或"失败，原因：XX"） |
| T4 | 不汇报，直接回答 |

**汇报时机（适用于 T1/T2/T3）：**
- 每完成一个步骤
- 审查不通过时（汇报问题、回退计划）
- 遇到阻塞时（汇报原因、需要用户决策的事项）
- 所有任务完成时（汇报最终结果）

**T1 完整汇报模板：**
```
📋 进度汇报
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⏱️ 时间：YYYY-MM-DD HH:MM
📍 当前阶段：Step X/6（步骤名称）

✅ 已完成：
  - Step 1: 需求理解（完成）
  - Step 2: 方案对决（完成）

🔄 进行中：
  - Step 3: 技术方案（执行中）

⏳ 待办：
  - Step 4: 编码执行
  - Step 5: 测试验收
  - Step 6: 完成报告

❓ 遇到的问题：
  - （如有）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**T2 简化汇报模板：**
```
📋 进度汇报
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📍 当前阶段：Step X/4（步骤名称）
🔄 下一步：（下一步计划）
❓ 遇到的问题：（如有）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 九、用户权限与控制机制

**核心原则：用户指令直接对主智能体下发，主智能体拥有完全控制权**

### 控制方式

| 控制方式 | 定义 | 行为 |
|---------|------|------|
| **停止** | 完全终止流程，需要用户重新启动 | 主智能体发起停止请求，所有子智能体尽快停止，流程终止 |
| **暂停** | 临时挂起流程，保存状态 | 主智能体发起暂停请求，所有子智能体尽快暂停，用户可随时恢复 |
| **质疑** | 对当前结果不满意，要求重新执行 | 主智能体智能判断用户意图，暂停当前步骤，展示结果供用户评审 |

### 停止机制

```
用户说"停止"
    ↓
主智能体发起停止请求（子智能体可能需要几秒到几十秒的过渡时间）
    ↓
主智能体向用户报告当前状态（已完成步骤、停止原因、建议下一步行动）
    ↓
流程终止，等待用户手动重启
```

### 暂停机制

```
用户说"暂停"
    ↓
主智能体发起暂停请求，保存当前状态
    ↓
主智能体向用户报告当前状态（已完成步骤、恢复指令）
    ↓
用户说"继续" → 主智能体恢复执行
```

### 暂停后用户指令

| 用户指令 | 主智能体行为 |
|---------|---------|
| **继续** | 从暂停点继续执行 |
| **重新开始** | 回退到当前步骤开始，重新执行 |
| **调整方案** | 重新讨论方案，可能回退到 Step 2 |
| **结束** | 终止整个流程，输出最终报告 |
| **修改需求** | 回退到 Step 1，重新理解需求 |

---

## 十、技能与工具使用规则

**技能（如 OpenViking 记忆，通过 `ov` CLI 使用）是辅助工具，不是工作流的触发者。**

### 技能定位：
- 技能是主智能体的**可选工具**，用于增强特定能力（记忆查询等）
- 工作流由**用户说话**触发，主智能体判断任务级别后执行对应流程
- 技能不定义、不触发、不改变工作流

### 使用原则：
- 需要时使用，不需要时不使用
- 技能结果仅供参考，主智能体做最终判断
- 技能不可用时，用其他方式完成任务

---

## 用户项目列表

| 项目 | 路径 |
|------|------|
| AI-RAG-KnowledgeBase | C:\Users\10359\Desktop\AI-RAG-KnowledgeBase |
| Public-Work | C:\Users\10359\Desktop\Public-Work |
| AutoTestHub | C:\Users\10359\Desktop\AutoTestHub |
| AI_bian_yuan_fu_wu_qi_yun_wei | C:\Users\10359\Desktop\AI_bian_yuan_fu_wu_qi_yun_wei |
| 2048 | C:\Users\10359\Desktop\2048 |
| xu_qiu_zhuan_huan_yong_li | C:\Users\10359\Desktop\xu_qiu_zhuan_huan_yong_li |
| Grimoire-Echoes | C:\Users\10359\Desktop\Grimoire-Echoes |

---

## 附录：规则同步（自动）

**规则已自动化同步：计划任务 `Rules-AutoSync` 在登录时 + 每 5 分钟自动把本母本（AGENTS.md + CLAUDE.md，两文件内容相同）分发到 7 个项目，修改母本后约 5 分钟内全项目生效，无需手动操作。**

- 变更/错误日志：`AI-RAG-KnowledgeBase\scripts\rules-autosync.log`（无变化时每天只记一条心跳）
- 每次运行明细：`AI-RAG-KnowledgeBase\scripts\sync-log.txt`

手动立即同步（可选，改完想立刻生效时用）：

```powershell
powershell -ExecutionPolicy Bypass -File "C:\Users\10359\Desktop\AI-RAG-KnowledgeBase\scripts\sync-Codex-rules.ps1"
```

校验一致性（只检查不修改）：

```powershell
powershell -ExecutionPolicy Bypass -File "C:\Users\10359\Desktop\AI-RAG-KnowledgeBase\scripts\sync-Codex-rules.ps1" -CheckOnly
```

---

*此文件是最高优先级配置，每次新会话必须加载*
