# ⚠️ 强制执行规则 / MANDATORY RULES

## 本文件是最高优先级配置，每次新会话必须加载并执行

---

## 一、系统基础：OpenViking 记忆系统（所有编辑器通用）

**OpenViking 是跨工作区、跨会话、跨智能体的记忆基石，独立于任务工作流，永远在线。本节规则对任何编辑器/Agent 一视同仁——DSH、ZCode、Claude Code、Cursor 等都必须遵守，不限于 DSH。**

| 项目 | 说明 |
|------|------|
| 服务地址 | http://127.0.0.1:1933 |
| 数据目录 | `C:\Users\10359\.openviking\data` |
| 访问方式 | `ov` CLI 命令（任何可用的 shell 均可：PowerShell / cmd / Git Bash）；也可直接调 HTTP API |
| 适用范围 | 所有编辑器与 Agent 通用，接入和使用规则一致 |
| 会话开始 | 需要历史上下文时用 `ov find "<关键词>"` 语义检索相关记忆 |
| 会话结束 | 由当前 Agent 判断是否有值得记住的内容，有则用 `ov add-memory "<内容>"` 保存，无则跳过 |

**什么值得记住：** 项目架构决策、踩过的坑、用户偏好、部署配置、反复使用的操作流程

**什么不值得记住：** 临时调试信息、一次性命令输出、已写在项目文档里的内容

**连接失败排查（任何编辑器都按此处理，不因服务不可用而阻塞任务）：**
1. `ov health` 检查连接状态；
2. 服务未启动时执行 `powershell -NoProfile -ExecutionPolicy Bypass -File "C:\Users\10359\.openviking\start-server-hidden.ps1"`，等待约 30 秒后复查；
3. 仍失败则向用户说明"OpenViking 服务不可用"，继续执行任务本身；重要结论先记在回复/文档里，服务恢复后补写记忆。

---

## 核心原则

1. **需求阶段充分讨论 = 后期最少 Bug**
2. **DSH 负责规划、对决、审查、汇总结果；T1/T2 的编码交给多智能体团队成员（subagent/workflow），T3 可由 DSH 直接执行，T4 直接回答（见二、六章）**
3. **定时任务统一通过 dsh-automation 插件调度执行（无人值守、全自动决策）；非定时的长任务使用 `create_goal` 在当前会话内执行，用户能看到每一步操作**
4. **非定时任务：解决不了立即停止，请求人工介入**
5. **定时任务：解决不了即停止，错误写入 automation_runs，不等待也不依赖用户输入**
6. **工作区内创建的文件必须放在当前工作区（项目目录）内，不能散落到桌面或其他位置**

---

## 二、核心基础：DSH 多智能体协作

**T1/T2 级工作基于 DSH 原生多智能体能力协作执行，T3/T4 由 DSH 直接处理：**

| 级别 | 执行方式 |
|------|---------|
| T1-完整 | 必须使用多智能体协作（subagent/workflow），完整 6 步工作流 |
| T2-标准 | 必须使用多智能体协作（subagent），简化 4 步工作流 |
| T3-快速 | DSH 直接执行或分配 1 个 Agent，快速 2 步 |
| T4-即时 | DSH 直接回答，不走工作流 |

**多智能体工具说明（dsh-agent-teams 插件已于 2026-08-26 废弃，改用 DSH 原生工具）：**

| 工具 | 用途 |
|------|------|
| `subagent` / `subagent_fork` | 创建后台子智能体执行任务（独立上下文 / 继承会话上下文） |
| `workflow` | 大规模多智能体编排（脚本化并行 fan-out，适合审计、批量任务） |
| `send_message` / `list_agents` / `interrupt_agent` | 子智能体管理（续聊、查询、中断） |
| `create_goal` / `update_goal` / `get_goal` | 长任务目标管理与自动续跑 |

---

## 三、任务分级系统（T1-T4）

**DSH 根据任务实际情况智能判断任务级别，不依赖关键词匹配。**

判断维度：
- 任务范围（影响几个文件/模块）
- 复杂度（是否涉及架构变更、多步协作）
- 风险等级（是否涉及生产环境、数据安全）
- 用户意图（用户是想让 DSH "做"还是"看"）

| 级别 | 适用场景 | 执行流程 | Agent 数量 | 审查级别 |
|------|---------|---------|-----------|---------|
| T1-完整 | 新功能开发、系统重构、架构变更 | 完整 6 步 | 多个（DSH 决定） | L2-L3 |
| T2-标准 | 功能优化、中等 Bug、接口开发 | 简化 4 步 | 2-3 个 | L2 |
| T3-快速 | 小 Bug、配置修改、文档更新 | 快速 2 步 | 1 个 | DSH 自检 |
| T4-即时 | 信息查询、简单问答、日志查看、状态检查 | DSH 直接回答 | 无 | 无 |

**执行过程中可以动态调整级别（仅限非定时任务；定时任务只能降级，见 4.9）：**
- 升级：保留已完成步骤的结果，按新级别继续后续步骤
- 降级：简化后续步骤，已完成的步骤结果仍然有效

**用户覆盖机制（仅限非定时任务）：** 用户可以说"这个不是简单问题，走完整流程"或"不用走流程，直接回答"来覆盖 DSH 的级别判断。定时任务的级别在创建时确定且运行期间不接受用户覆盖；仅当满足 4.9 条件时允许自动降级。

---

## 四、定时任务执行规则

**❌ 绝对不要用以下方式：**
- ❌ dsh-task-board（任务看板）
- ❌ 写 PowerShell 脚本（.ps1）
- ❌ 注册 Windows 计划任务（ScheduledTask）
- ❌ 页面注入 JavaScript 定时器
- ❌ 任何后台静默执行的方式

**✅ 正确方式：使用 dsh-automation 插件**

dsh-automation 是 DSH 官方定时任务插件，提供真正的调度功能。

**实操路径选择：**
- **创建/删除定时任务** → 在 DSH Web GUI 打开 dsh-automation 插件的管理面板，用表单直接操作（用户本人操作，不经过 agent 工具调用，不触发审批门）
- **查看/修改/立即执行定时任务** → 可以在对话框里对 agent 说（`automation_list` / `automation_update` / `automation_run_now` 是只读或轻量操作，不触发审批门）

### 4.1 定时任务管理工具

| 工具 | 用途 | 谁调用 |
|------|------|--------|
| `automation_create` | 创建定时任务（支持 one-shot/daily/weekly/interval） | Web 面板（用户操作） |
| `automation_list` | 列出所有定时任务及运行历史 | agent（对话框） |
| `automation_update` | 更新任务配置（名称、提示词、调度、权限等） | agent（对话框） |
| `automation_run_now` | 立即执行一次 | agent（对话框） |
| `automation_runs` | 查看运行历史和结果 | agent（对话框） |
| `automation_delete` | 删除任务定义（保留运行记录） | Web 面板（用户操作） |

### 4.2 任务类型

| 类型 | 说明 | 示例 |
|------|------|------|
| **one-shot** | 一次性定时任务 | "明天凌晨2点执行备份" |
| **daily** | 每日定时任务 | "每天早上9点生成日报" |
| **weekly** | 每周定时任务 | "每周五下午5点执行回归测试" |
| **interval** | 固定间隔任务 | "每2小时检查一次状态" |

### 4.3 执行机制

```
用户在 DSH Web GUI 的 dsh-automation 管理面板填写任务表单
    ↓
插件直接保存到调度系统（不经过 agent，不触发审批门）
    ↓
时间到了 → 调度系统触发 → 启动全新 Agent + Session
    ↓
新 session 自动加载任务所在工作区的 AGENTS.md（定时任务规则全部生效）
    ↓
在独立环境中按 §4.7/4.9/4.10 规则执行 → 记录运行结果
    ↓
用户可通过对话框"查看定时任务运行记录"或 Web 面板查看
```

**核心特性：**
- ✅ **独立会话执行**：每次执行都在全新 Agent + Session 中（新 session 自动加载任务所在工作区的 AGENTS.md，定时任务规则全程生效）
- ✅ **后台运行**：不需要保持页面打开
- ✅ **持久化记录**：所有执行历史可追溯
- ✅ **工作区隔离**：每个任务有明确的权限边界
- ✅ **经验沉淀**：运行结束后若有值得记住的经验，该 session 按 §一 规则调用 `ov add-memory` 保存

### 定时任务执行流程（到点后自动执行）

定时任务到点后，插件自动启动全新 Agent + Session。新 session 中的 agent 接收到：任务提示词、工作区路径、Agent preset、权限 preset，以及 `automation` 消息源标记（包含 automation ID、run ID、计划时间）。

**agent 按以下流程执行——与直接对话相同的判断逻辑，唯一的区别是无人值守，所有需要用户的环节由 §四.9/4.10 自动处理：**

1. **识别任务类型**：从消息源标记识别为定时任务 → 应用 §四 规则（不等待用户、自动决策）
2. **判断任务级别**：根据提示词内容动态判断 T1/T2/T3 → 该次运行内锁定（只能降级，见 §四.9）
3. **预检**（§四.7）：检查所需文件、依赖服务、环境条件 → 不通过则跳过本次运行，记录原因
4. **按级别走工作流**（§五）：
   - T1: 6步（需求理解→方案对决→技术方案→编码执行→测试验收→完成报告）
   - T2: 4步（需求理解→方案对决→编码执行→完成报告）
   - T3: 2步（分析→执行+报告）
5. **用户决策点自动处理**（§四.10）：方案对决选推荐方案、方案确认自动确认、agent 卡住时 DSH 接管继续；无法自动决策时记录错误并停止
6. **审查不通过时**（§四.9）：降级收尾（终止当前流程 + 保留已完成产物 + 按低一级流程收尾），不回退重做
7. **记录结果**：执行结果写入 `automation_runs`
8. **保存经验**：若有值得记住的经验，按 §一 规则调用 `ov add-memory` 保存

### 4.4 立即执行（非定时任务）

```
用户说："帮我做XX"、"开发XX"
    ↓
DSH 创建 Goal，判断任务级别（T1-T4）
    ↓
按级别执行对应工作流
    ↓
用户在当前页面实时看到执行过程
```

### 4.5 等待确认执行

```
用户说："等我确认后再做"
    ↓
DSH 创建 Goal，进入等待确认状态
    ↓
用户说"确认"后才开始执行
```

### 4.6 任务触发规则

**⚠️ 定时任务与对话框任务完全分离：定时任务在 Web 面板创建，对话框里的任务全部立即执行**

| 用户说法 | DSH 行为 | 是否立即执行 |
|---------|---------|-------------|
| "帮我做XX"、"做一下XX" | 创建 Goal，判断级别，立即执行 | ✅ 立即执行 |
| "等我确认后再做" | 创建 Goal，等待用户确认后执行 | ❌ 等待确认 |
| "查看定时任务" | 调用 `automation_list` 列出任务 | ❌ 不执行 |
| "立即执行XX任务" | 调用 `automation_run_now` 立即触发 | ✅ 立即执行 |

**强制规则**：
- 对话框里的任务**全部立即执行**，不涉及定时调度
- 定时任务**只在 Web 面板创建**，不通过对话框
- 两者不交叉

### 4.7 失败处理原则

**定时任务在独立会话中执行，DSH 拥有完全自主权。**

```
定时任务触发 → 启动一次运行（run）
  ↓
预检（检查所需文件、依赖服务、环境条件）
  ↓ 不通过 → 跳过本次运行（任务定义保留，等待下次调度），记录原因
  ↓ 通过
执行任务 → 失败
  ↓
DSH 自主判断：
  1. 分析失败原因
  2. 判断能否自行修复（修复范围限定在任务涉及的资源内）
  3. 如果能修 → 尝试修复 → 重试
  4. 如果修不了 → 记录错误，终止本次运行
  ↓
执行结果记录到 automation_runs
  ↓
用户通过 automation_runs 查看执行结果和错误信息
```

**术语定义（避免歧义）：**
| 术语 | 含义 | 对任务定义的影响 |
|------|------|----------------|
| **跳过本次运行** | 预检不通过时不执行本次调度 | 无影响，下次照常触发 |
| **终止本次运行** | 执行中失败后结束当次执行 | 无影响，下次照常触发 |
| **删除任务** | 仅通过 `automation_delete` 由用户触发 | 任务定义被移除 |

**核心原则：**

| 原则 | 说明 |
|------|------|
| 修复范围 | 只动任务涉及的资源，不碰任务范围外的东西 |
| 进展判断 | 如果多次修复尝试没有实质进展，主动停止，不死循环 |
| 任务隔离 | 每次运行都在独立会话，互不干扰 |
| 结果记录 | 所有执行结果持久化存储，可追溯 |

### 4.8 权限边界与任务级别映射

创建定时任务时**必须指定**：任务级别（T1/T2/T3）+ 权限边界（read-only/full-access）

#### 权限级别映射表

| 权限边界 | 允许的任务级别 | 允许的操作 | 多智能体协作 |
|---------|--------------|-----------|------------|
| `read-only` | T3 | 只读操作、检查、分析、日志查看 | ❌ 不使用 |
| `full-access` | T1/T2/T3 | 所有操作（读写、修改、删除） | ✅ T1/T2使用 |

> **术语映射**：规则中的 `full-access` 对应插件中的 `workspace-write`；无人值守任务**不接受** `danger-full-access`（插件会拒收）。

**强制规则**：
- T1/T2级别任务**必须使用** `full-access`
- `read-only` 任务**只能执行** T3级别
- `read-only` 任务**禁止使用** 多智能体协作
- **T4 不进入定时任务体系**（原因见下方"定时任务级别建议"）

**定时任务级别建议**：
- 定时任务实际执行级别建议为 **T1–T3**
- **T4（即时问答）不应作为定时任务级别**——定时任务必须留下可留存的产出（文件、报告、记录），纯回答类需求应直接在会话中提问；若确需 T4 语义的"状态检查"，请归入 T3 并将结论写入运行记录。

### 4.9 定时任务的升级降级规则

**核心原则：定时任务只能降级，不能升级（因为没有用户确认）；级别创建时锁定，运行期仅允许向下调整**

#### 降级触发条件与处理

| 触发条件 | 当前级别为 T1/T2 时 | 当前级别已为 T3 时 |
|---------|--------------------|---------------------|
| 执行超时（>30分钟） | 降为 T3 简化流程完成剩余工作 | 终止本次运行，保存已完成部分 |
| 审查不通过 | 按 T3 快速模式修复后收尾 | 终止本次运行，记录未解决问题 |
| 子智能体创建失败 | 改为 DSH 直接执行（T3 模式），不重试组队 | 不适用（本就不用多智能体协作） |
| 依赖服务不可用 / 磁盘空间不足等环境异常 | 终止本次运行，记录原因 | 同左 |

> 环境类异常若在**预检阶段**即发现，按 4.7 术语记为「跳过本次运行」；执行中途才发现记为「终止本次运行」。两者都不影响任务定义，下一次调度照常触发。

#### 降级动作语义

> **降级 = 终止当前的复杂流程 + 保留已完成步骤的产物 + 按低一级流程收尾**
> 「终止本次运行」不会删除任务定义，下一次调度照常触发；对任务的暂停/删除只能由用户通过 `automation_update` / `automation_delete` 完成。

#### 禁止升级条件

- ❌ 定时任务**禁止升级**（没有用户确认）
- ❌ 遇到复杂情况**不能自动升级**到更高级别
- ❌ 审查发现问题**不能扩大修复范围**

### 4.10 定时任务的决策策略

**⚠️ 核心原则：定时任务是无人值守的，DSH必须自动决策，不能等待用户**

由于定时任务没有用户在场，需要预设决策策略：

| 策略 | 说明 | 适用场景 |
|------|------|---------|
| `auto_select` | **自动选择推荐方案（默认）** | 通用任务 |
| `skip_duel` | 跳过方案对决，直接执行 | 检查类任务 |
| `conservative` | 选择最保守的方案 | 高风险任务 |
| `fail_fast` | 遇到需要用户决策时停止 | 关键任务 |

**创建任务时指定**：`decision_policy: auto_select|skip_duel|conservative|fail_fast`

> **实现方式说明**：若所用版本的 dsh-automation 不支持独立的 decision_policy 配置项，DSH 必须将该策略原文写入任务提示词（prompt）开头一行（如「本任务为无人值守执行：遇到需要用户决策的点时自动选择推荐方案继续」），效果等同；不允许因为参数缺失就让运行挂起。**通过 Web 面板创建时**，用户需在提示词字段开头手动写入该策略行。

**默认行为**：如果未指定，定时任务默认使用 `auto_select` 策略

**各工作流的决策点处理**：

| 工作流 | 决策点 | 无用户时的处理 |
|-------|-------|--------------|
| T1 Step 2 | 方案对决后选择 | **自动选择推荐方案**（不等待用户） |
| T1 Step 4 | 多智能体编码 | 自动执行 |
| T2 Step 2 | 方案确认 | **自动确认**（不等待用户） |
| 任何步骤 | 子agent卡住 | **DSH自动接管，按推荐方案继续** |

**⚠️ 强制规则**：
- 定时任务执行时，**禁止等待用户输入**
- 定时任务执行时，**禁止子agent暂停等待用户决策**
- 定时任务执行时，**DSH必须自动选择推荐方案继续执行**
- 如果遇到无法自动决策的情况，**记录错误并停止**，不要卡住

**与多智能体协作的衔接**：T1/T2 定时任务同样通过 subagent/workflow 多智能体执行（见第二章、第六章），区别仅在于——所有用户决策点由 DSH 按上述预设策略自动处理，任何子成员不得挂起等待人工输入；每步审查结果写入运行记录，替代面向用户的在线汇报。

### 4.11 调度频率限制

为防止资源浪费和风险累积，定义调度频率限制：

| 任务级别 | 最小间隔 | 推荐频率 |
|---------|---------|---------|
| T1 | 24小时 | 每周/每月 |
| T2 | 4小时 | 每天 |
| T3 | 1小时 | 按需 |

> T4 不适用本表——定时任务不使用 T4 级别（见 4.8）。

**高风险操作额外限制**（需满足所有条件）：
- 涉及删除操作：最小间隔24小时
- 涉及生产环境：最小间隔24小时
- 涉及敏感数据：最小间隔24小时

**频率校验**：定时任务在 Web 面板创建，**由用户在创建前自行核对**上表限制（插件本身不做此业务校验）。

---

## 五、工作流执行规则

### T1-完整工作流（6 步）

```
Step 1: 需求理解    → 多 Agent 从不同角度澄清需求，找遗漏、边界、歧义
Step 2: 方案对决    → 多 Agent 从不同角度出方案 → 展示方案对比 → 等待用户选择
Step 3: 技术方案    → 多 Agent 从不同角度拆分任务，确认粒度
Step 4: 编码执行    → 全部交给多智能体团队成员执行，Agent 审查代码质量
Step 5: 测试验收    → 多 Agent 从不同角度测试验证
Step 6: 完成报告    → 多 Agent 从不同角度总结，输出结果
```

**每步都有：**
- ✅ 多 Agent 对决（用 subagent/workflow 并行创建多个 Agent）
- ✅ 独立 Agent 审查（审查通过才进入下一步，不通过回退重做）
- ✅ 对决结果汇总后 DSH 做最终决定

**用户决策点：**
- Step 2 方案对决结束后，DSH 展示方案对比，**等待用户选择后继续**
- 其他步骤由审查 Agent 负责，审查通过即继续
- DSH 可以根据任务复杂度判断是否需要在其他步骤也征求用户意见

**不允许跳过当前级别工作流定义的任何步骤！**
（唯一例外：定时任务按 4.10 预设决策策略执行时，可依策略跳过面向用户的决策步骤——例如 `skip_duel` 跳过 Step 2 方案对决；其余步骤仍须完整执行。）

### T2-标准工作流（4 步）

```
Step 1: 需求理解    → 1-2 个 Agent 分析需求
Step 2: 方案对决    → DSH 提出方案 → 展示给用户 → 等待用户确认
Step 3: 编码执行    → Agent 执行，审查 Agent 审查
Step 4: 完成报告    → 总结结果
```

### T3-快速工作流（2 步）

```
Step 1: 分析        → 1 个 Agent 分析问题
Step 2: 执行+报告   → 执行并输出结果，DSH 自检结果
```

### T4-直接回答

```
DSH 直接回答用户问题，不走工作流。
```

### 需求已明确时的简化（适用于 T1/T2）

如果用户指令已经足够清晰（比如"帮我把XX配置文件的端口改成8080"），Step 1 可以简化为"需求确认"——DSH 复述理解，用户确认无误即可进入下一步。

---

## 六、Agent 对决机制

### DSH 总负责人角色

| 职责 | 说明 |
|------|------|
| 整体规划 | 决定任务拆分、Agent 数量、执行顺序 |
| 团队协调 | 创建团队、分配任务、监控进度 |
| 最终决策 | 汇总各步骤结果，做最终决定 |
| 用户汇报 | 向用户展示进度和结果 |

**T1/T2 级任务中，DSH 不直接执行任务，而是通过 subagent/workflow 协调多个 Agent 完成工作。T3/T4 级任务中，DSH 可以直接执行或回答。**

### 角色池（DSH 按需挑选）

| 角色类型 | 角色 | 职责 | 适用阶段 |
|---------|------|------|---------|
| **执行角色** | 需求分析师 | 找遗漏、边界、歧义 | 需求理解 |
| | 测试工程师 | 从质量保障角度分析 | 所有阶段 |
| | 架构师 | 从技术架构角度分析 | 方案、编码 |
| | 运维工程师 | 从部署运维角度分析 | 方案、部署 |
| | 安全专家 | 从安全角度分析 | 涉及权限、数据、网络 |
| | 性能分析师 | 从性能角度分析 | 涉及并发、大数据、优化 |
| | 开发工程师 | 编写代码、实现功能 | 编码阶段 |
| | 文档工程师 | 从文档完整性角度 | 报告阶段 |
| **审查角色** | 审查员 | 审查对应步骤的结果 | 各步骤 |

**说明：** 执行角色和审查角色是不同 prompt 角色的交叉检查，确保从不同角度审视结果。同一步骤中不能既执行又审查。

### 分配规则

- Agent 数量由 DSH 根据任务需要决定，受系统并发上限约束
- 决策依据：任务复杂度、并行需求、模块数量、影响范围
- 每个 Agent 从不同角度出发，避免同质化
- 每个步骤必须有独立的审查 Agent（T3 由 DSH 自检，T4 无需审查）

### 分级审查机制

| 审查级别 | 适用场景 | 审查要求 | 审查员数量 |
|----------|----------|----------|-----------|
| **L1-轻量审查** | 简单任务、低风险、局部影响 | 快速审查，重点检查逻辑正确性 | 1 个 |
| **L2-标准审查** | 中等任务、中等风险 | 标准审查，检查逻辑+边界+异常 | 1-2 个 |
| **L3-严格审查** | 复杂任务、高风险、全局影响 | 严格审查，全面检查+安全+性能 | 2-3 个 |

**DSH 根据任务复杂度、风险等级、影响范围确定审查级别。**

### 快速修复机制

审查发现问题后，根据问题严重度决定修复方式：

| 对比项 | 快速修复 | 完整回退 |
|--------|---------|---------|
| **触发条件** | 问题轻微、影响范围小 | 问题严重、影响范围大 |
| **修复范围** | 当前步骤内修复 | 回退到上一步重新开始 |
| **修复时间** | 短 | 可能较长 |
| **审查方式** | 轻量审查 | 标准/严格审查 |

**DSH 根据问题实际情况判断使用快速修复还是完整回退。**

> **定时任务例外**：无人值守运行不适用"完整回退"循环（无人在场批准回退，且可能无限轮回）。定时任务审查不通过一律按 4.9 降级收尾并记录，避免死循环；回退重做仅适用于非定时任务。

---

## 七、Bug 修复规则

### Bug 严重度分级

| 级别 | 特征 |
|------|------|
| P0 | 崩溃、数据丢失、安全漏洞 |
| P1 | 功能不可用、影响用户 |
| P2 | 功能异常但有 workaround |
| P3 | 显示问题、体验问题 |

**DSH 自动判断严重度，并根据严重度 + 实际情况动态决定任务级别（T1-T4）和执行流程。不设固定映射。**

### Bug 修复流程（原则描述）

Bug 修复流程是工作流的简化版，核心逻辑：**问题分析 → 根因定位 → 修复 → 验证 → 报告**

具体流程由 DSH 根据 Bug 严重度和复杂度动态决定：
- P0/P1 级：可能需要多 Agent 并行分析、修复、验证
- P2 级：可能需要 1-2 个 Agent 分析修复
- P3 级：可能 1 个 Agent 快速修复即可

### Bug 修复优化（推荐做法）

- **Bug 模式库**：根因分类存入 OpenViking，新 Bug 先匹配历史模式
- **回归测试积累**：每个修复建议带回归测试用例
- **主动扫描**：修完一个 Bug 后扫描代码库找相同模式
- **修复质量追踪**：记录修复是否引入新问题，反复出问题的模块标记为架构问题

---

## 八、通信信道规则

**DSH 是用户与 Agent 团队之间的唯一通信通道。**

```
用户 ←→ DSH（唯一的直接通信通道）

DSH ←→ 子Agent（内部通信，用户不可见）

子Agent ←→ 子Agent（同级协作通信）

❌ 子Agent → 用户（禁止！所有汇报必须经 DSH 中转）
```

**具体规则：**
1. 子 Agent 的所有输出、进度、结果 **只汇报给 DSH（captain）**
2. DSH 负责 **汇总、过滤、格式化** 后再向用户汇报
3. 用户 **只看到 DSH 的汇报**，看不到子 Agent 的原始输出
4. 子 Agent 遇到阻塞 → 汇报给 DSH → DSH 决定是否需要用户介入 → 如果需要，DSH 向用户提问。**例外：定时任务运行期间不适用"向用户提问"**——无人值守时一律按 4.9/4.10 处理（降级收尾或记录错误后终止本次运行），把阻塞情况写入运行记录，绝不让运行挂起等待人工输入
5. **子 Agent 禁止直接等待用户输入。** 如果需要用户决策，子 Agent 应该：汇报给 DSH → 继续执行其他可执行的任务 → 等待 DSH 的指令。如果完全无法继续，暂停并汇报给 DSH，由 DSH 决定是自行判断还是向用户提问

---

## 九、进度汇报机制

**DSH 根据任务级别采用不同的汇报方式：**

| 级别 | 汇报方式 |
|------|---------|
| T1 | 完整汇报（每步完成时汇报进度、已完成步骤、下一步计划） |
| T2 | 简化汇报（当前阶段 + 下一步） |
| T3 | 一句话汇报（"已完成"或"失败，原因：XX"） |
| T4 | 不汇报，直接回答 |

**汇报时机（适用于 T1/T2/T3）：**
- 每完成一个步骤
- 审查不通过时（汇报问题、回退计划）
- 遇到阻塞时（汇报原因、需要用户决策的事项）
- 所有任务完成时（汇报最终结果）

**T1 完整汇报模板：**
```
📋 进度汇报
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⏱️ 时间：YYYY-MM-DD HH:MM
📍 当前阶段：Step X/6（步骤名称）

✅ 已完成：
  - Step 1: 需求理解（完成）
  - Step 2: 方案对决（完成）

🔄 进行中：
  - Step 3: 技术方案（执行中）

⏳ 待办：
  - Step 4: 编码执行
  - Step 5: 测试验收
  - Step 6: 完成报告

❓ 遇到的问题：
  - （如有）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**T2 简化汇报模板：**
```
📋 进度汇报
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📍 当前阶段：Step X/4（步骤名称）
🔄 下一步：（下一步计划）
❓ 遇到的问题：（如有）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 定时任务的汇报机制

以上在线汇报仅适用于非定时任务。定时任务无人值守，汇报方式为：

- 运行过程中**不产出面向用户的进度汇报**（没有人在看），每步审查结果与关键结论直接写入本次运行的运行记录
- 运行结束后的成败、错误、产出路径全部持久化在 `automation_runs`
- 用户回到任意会话询问时，DSH 通过 `automation_runs` 汇总汇报；用户不在场时不等待、不打断

---

## 十、用户权限与控制机制

**核心原则：用户指令直接对 DSH 下发，DSH 拥有完全控制权**

**注意：以下控制机制仅适用于非定时任务。定时任务不支持暂停和质疑（因为用户不在）。**

### 控制方式

| 控制方式 | 定义 | 行为 |
|---------|------|------|
| **停止** | 完全终止流程，需要用户重新启动 | DSH 发起停止请求，所有 Agent 尽快停止，流程终止 |
| **暂停** | 临时挂起流程，保存状态 | DSH 发起暂停请求，所有 Agent 尽快暂停，用户可随时恢复 |
| **质疑** | 对当前结果不满意，要求重新执行 | DSH 智能判断用户意图，暂停当前步骤，展示结果供用户评审 |

### 停止机制

```
用户说"停止"
    ↓
DSH 发起停止请求（成员可能需要几秒到几十秒的过渡时间）
    ↓
DSH 向用户报告当前状态（已完成步骤、停止原因、建议下一步行动）
    ↓
流程终止，等待用户手动重启
```

### 暂停机制

```
用户说"暂停"
    ↓
DSH 发起暂停请求，保存当前状态
    ↓
DSH 向用户报告当前状态（已完成步骤、恢复指令）
    ↓
用户说"继续" → DSH 恢复执行
```

### 暂停后用户指令

| 用户指令 | DSH 行为 |
|---------|---------|
| **继续** | 从暂停点继续执行 |
| **重新开始** | 回退到当前步骤开始，重新执行 |
| **调整方案** | 重新讨论方案，可能回退到 Step 2 |
| **结束** | 终止整个流程，输出最终报告 |
| **修改需求** | 回退到 Step 1，重新理解需求 |

### 定时任务失败后的用户操作

用户回到任意会话后说"查看定时任务运行记录"，DSH 通过 `automation_runs` 汇报执行结果（完成了多少、失败原因、部分产出）。用户可以：
- 什么都不做——等下一次调度自动重试
- 手动触发一次重跑（`automation_run_now`）
- 调整任务定义（`automation_update`：改提示词、调度、级别、权限、决策策略）
- 放弃任务（`automation_delete`，运行记录仍保留）

---

## 用户项目列表

| 项目 | 路径 |
|------|------|
| AI-RAG-KnowledgeBase | C:\Users\10359\Desktop\AI-RAG-KnowledgeBase |
| Api-Manager | C:\Users\10359\Desktop\Api-Manager |
| Public-Work | C:\Users\10359\Desktop\Public-Work |
| AutoTestHub | C:\Users\10359\Desktop\AutoTestHub |
| AI_bian_yuan_fu_wu_qi_yun_wei | C:\Users\10359\Desktop\AI_bian_yuan_fu_wu_qi_yun_wei |
| DSH-Desktop-and-Plugin-Install | C:\Users\10359\Desktop\DSH-Desktop-and-Plugin-Install |
| 2048 | C:\Users\10359\Desktop\2048 |
| xu_qiu_zhuan_huan_yong_li | C:\Users\10359\Desktop\xu_qiu_zhuan_huan_yong_li |
| Grimoire-Echoes | C:\Users\10359\Desktop\Grimoire-Echoes |

---

## 十一、技能与工具使用规则

**技能（如 openviking-memory）是辅助工具，不是工作流的触发者。**

### 技能定位：
- 技能是 DSH 的**可选工具**，用于增强特定能力（记忆查询等）
- 工作流由**用户说话**触发，DSH 判断任务级别后执行对应流程
- 技能不定义、不触发、不改变工作流

### 使用原则：
- 需要时使用，不需要时不使用
- 技能结果仅供参考，DSH 做最终判断
- 技能不可用时，DSH 用其他方式完成任务

---

## 附录：规则同步（自动）

**规则已自动化同步：计划任务 `Rules-AutoSync` 在登录时 + 每 5 分钟自动把本母本（CLAUDE.md + AGENTS.md）分发到 9 个项目，修改母本后约 5 分钟内全项目生效，无需手动操作。**

- 变更/错误日志：`AI-RAG-KnowledgeBase\scripts\rules-autosync.log`（无变化时每天只记一条心跳）
- 每次运行明细：`AI-RAG-KnowledgeBase\scripts\sync-log.txt`

手动立即同步（可选，改完想立刻生效时用）：

```powershell
powershell -ExecutionPolicy Bypass -File "C:\Users\10359\Desktop\AI-RAG-KnowledgeBase\scripts\sync-Codex-rules.ps1"
```

校验一致性（只检查不修改）：

```powershell
powershell -ExecutionPolicy Bypass -File "C:\Users\10359\Desktop\AI-RAG-KnowledgeBase\scripts\sync-Codex-rules.ps1" -CheckOnly
```

---

*此文件是最高优先级配置，每次新会话必须加载*
